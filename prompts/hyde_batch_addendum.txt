You will receive MULTIPLE independent patient summaries in this one request, each labeled with an index number. Write one trial description per patient, completely independently of the others — do not blend details across patients.

Return a JSON object with a single field "descriptions": an array with exactly one entry per patient given, each entry carrying its own "index" field (matching the label below) and a "text" field with that patient's trial description.
