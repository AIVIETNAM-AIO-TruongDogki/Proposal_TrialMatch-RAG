# TrialMatch-RAG — moi truong (Phase 0-2)
#
#   uv venv && uv pip install -r requirements.txt
#
# Pin cung phien ban: ket qua retrieval phai tai lap duoc o Phase 11, ma mot
# ban nang cap am tham cua scorer hay cua numpy du de lam lech con so.

# Cham diem. KHONG tu viet nDCG — quy uoc gain va nguong nhi phan cua trec_eval
# deu de doan sai (xem src/eval/verify.py).
pytrec_eval-terrier==0.5.10

# Kiem dinh paired bootstrap
numpy==2.5.2
scipy==1.18.1

# Doc corpus / benchmark
lxml==6.1.2
ir_datasets==0.6.3
tqdm==4.70.0

# Backend Gemini (Phase 4) — round-robin 3 API key, doc tu .env, khong bao gio
# hardcode key vao code
google-genai==2.20.0
python-dotenv==1.2.3

# Goi HTTP truc tiep (ollama.py) — da la dependency bac cau qua pyserini nhung
# chua tung duoc khai bao rieng
requests==2.34.2

# Tai lai rawdata/ tu Hugging Face Hub tren mot may moi (src/fetch_data.py)
huggingface_hub==1.29.0
